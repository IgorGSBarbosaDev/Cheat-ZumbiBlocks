<Window x:Class="ZB2SecurityLab.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZB2 Security Lab" Height="620" Width="820"
        MinHeight="560" MinWidth="720"
        WindowStartupLocation="CenterScreen"
        Background="#0D1117" Foreground="#E6EDF3">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="Background" Value="#238636" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Window.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="ZB2 Security Lab" FontSize="30" FontWeight="SemiBold" />
            <TextBlock Text="Launcher temporário para o build autorizado do Zumbi Blocks 2"
                       Foreground="#8B949E" FontSize="15" Margin="0,6,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Background="#161B22" CornerRadius="8" Padding="20" Margin="0,0,0,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Text="Steam" Foreground="#8B949E" Margin="0,0,12,12" />
                <TextBlock Grid.Column="1" Text="{Binding SteamPath}" Margin="0,0,0,12" />
                <TextBlock Grid.Row="1" Text="Instalação" Foreground="#8B949E" Margin="0,0,12,12" />
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding GamePath}" Margin="0,0,0,12" />
                <TextBlock Grid.Row="2" Text="Compatibilidade" Foreground="#8B949E" Margin="0,0,12,0" />
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding BuildStatus}" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="2">
            <Border Background="#161B22" CornerRadius="8" Padding="20">
                <StackPanel>
                    <TextBlock Text="Estado" Foreground="#8B949E" />
                    <TextBlock Text="{Binding Status}" FontSize="17" Margin="0,6,0,0" />
                    <ProgressBar IsIndeterminate="True" Height="4" Margin="0,16,0,0"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Border Background="#3D1518" CornerRadius="5" Padding="12" Margin="0,16,0,0"
                            Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="{Binding Error}" Foreground="#FFB3B8" />
                    </Border>
                </StackPanel>
            </Border>

            <CheckBox Content="Habilitar Mutation Mode somente nesta execução"
                      IsChecked="{Binding MutationEnabled}"
                      IsEnabled="{Binding CanLaunch}"
                      FontSize="15" Margin="4,22,0,6" />
            <TextBlock Text="Restrito ao laboratório autorizado. Começa desabilitado em toda nova execução."
                       Foreground="#8B949E" Margin="28,0,0,0" />
            <TextBlock Text="F8 abre o painel no jogo. O launcher não adiciona novos experimentos e não altera saves ou tráfego de rede."
                       Foreground="#8B949E" Margin="4,18,0,0" />
        </StackPanel>

        <Grid Grid.Row="3" Margin="0,24,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Content="Iniciar ZB2 Security Lab" Command="{Binding StartCommand}" />
                <Button Content="Tentar novamente" Command="{Binding RetryCommand}" Background="#30363D" />
                <Button Content="Abrir logs" Command="{Binding OpenLogsCommand}" Background="#30363D" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="Dados preservados em LocalAppData" Foreground="#8B949E"
                       VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Window>
